---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Katex from '../../components/Math.astro';
---

<BaseLayout pageTitle="upnt - About">
	<Fragment slot="title">
		QUBOとは
	</Fragment>
	<Fragment slot="main">
		<Katex  display=true expression="E(X) = \\sum_{i} Q_{ii}x_i + \\sum_{i,j} Q_{ij} x_ix_j" />
	</Fragment>
</BaseLayout>
